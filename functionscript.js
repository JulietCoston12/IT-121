function  validateform () {
  let Firstname = "Lastname,password,message"
document.getElementById("Firstname");
// add validatedPassword function here
console.log("message") ;
//UserActivationvalidatedForm (myform) => {
// return firsname.value.length;  
document.getElementById("demo").innerHTML =("validatedForm").length; 
  console.log(ValidatedForm);
}

function validatepassword(){
let password =  document.getElementById("password").value;

 // validatePassword should return true if a password is valid, and false otherwise.
//Invoke this new function from validateForm function,
let  length = password.length;// ('Function', 'Validated', 'Form');
// thatGuy.print();
let Firstname =,  Lastname,  message, passwoerd = "myForm";
console.log()("message");
// add validatedPassword function here
console.log("password") ;
//UserActivationvalidatedForm (myform) => {

 myform.firstname.value; // get text of firstname field

  // update the "message" Div with the approriate wording

document.getElementById("message").innerHTML = Firstname.toString('...');
console.log(Firstname);
console.log(Lastname)
  $("message").HTML("buttom clicked");

  for(let i  = 0; i <str.lenght; i++) {
  if ("Firstname, Lastname, message, password");
  string.paasword.indexOf(Str[i]  =-1) 
     return false;
  } document.querySelectorAll("Firsrname, Lastname, message, password");
    
  }
  